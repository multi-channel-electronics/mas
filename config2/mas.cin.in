<?php

/* install prefix */
$prefix = "@prefix@";

/* The network host and port for the logger daemon */
$log_address = "localhost:7884";

/* The location of the logfile on the logger host */
$log_file    = "/data/cryo/current_data/log";

/* The log level */
$log_level   = 1;

/* The number of fibre cards in the system */
$n_card      = @MAX_FIBRE_CARD@;

/* The list of MCEs we're prepared to deal with, in order.
 * This is a comma separated list of elements, each of which is:
 * (1) a number, N, indicating the local MCE talking to logical
 *     fibre card #N;
 * (2) "HOST:N", indicating MCE device #N on remote computer HOST,
 *     which is running mcenetd.  (Note: this is not necessarily the
 *     MCE talking to logical card #N on HOST, but depends on how the
 *     remote host enumerates it's devices.)
 */
$mce_device_list = "@MCE_LIST@";

/* directories */
$etc_dir = "@MAS_ETCDIR@";
$mas_root = @MAS_ROOTDIR@;
$mas_temp = "@MAS_TEMPDIR@";
$dataroot_list = "@MAS_DATAROOTLIST@";
$data_dir = "@MAS_DATADIR@";

/* Is this a mcenetd server? */
$mcenetd_server = @MCENETD_SERVER@;

/* And execute. */

include 'components/mas';

?>
