
#!/bin/bash

DRIVER=mce_dsp

SV=`uname -r | sed 's/\./\ /g' |awk '{print $2;}'`

grep '^mce_dsp\ ' /proc/modules && \
    sudo rmmod ${DRIVER}

if [ $SV = "6" ]; then
sudo insmod ${DRIVER}.ko
else
./remove_usb
sudo insmod ${DRIVER}.o
fi