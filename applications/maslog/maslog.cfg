/* Configuration file for MAS logging
   Note the separate sections for server and clients */

log_client:
{

	log_address = "localhost:7894";

};

log_server:
{

	log_address = "localhost:7894";

	log_file = "log";

	daemon = 0;

	level = 3;
};
